<div class="container mt-2">
  <div class="row mb-4">
    <div class="col-12">
      <div class="display-header d-flex justify-content-between align-items-center pb-3">
        <div>
          <h2 class="display-7 text-dark text-uppercase mb-0">
            {{#if currentCategory}}
              {{currentCategory.name}}
            {{else}}
              PRODUCT LIST
            {{/if}}
          </h2>
          <small class="text-muted mt-2">{{products.length}} products found</small>
        </div>
      </div>
      <hr class="my-2">
    </div>
  </div>

  <div class="row">
    <aside class="col-lg-3 mb-5 mb-lg-0">
      <div class="sidebar-widget">
        <h5 class="widget-title text-uppercase mb-4 pb-2" style="border-bottom: 2px solid var(--primary-color); padding-bottom: 10px;">
          Sort By
        </h5>
      </div>
    </aside>

    <div class="col-lg-9">
      {{#if (eq products.length 0)}}
        <div class="alert alert-info text-center py-5 rounded">
          <h5 class="mb-3">No products found</h5>
          <a href="/" class="btn btn-primary">Back to Home</a>
        </div>
      {{else}}
      
      <div class="row g-4">
        {{#each products}}
        <div class="col-md-6 col-xl-4">
          <div class="product-card h-100 position-relative shadow-sm rounded overflow-hidden">
            
            <div class="image-holder position-relative overflow-hidden" style="height: 250px;">
              <a href="/products/{{this.id}}">
                {{#if this.thumbnail}}
                  <img src="/static/{{this.thumbnail}}" alt="{{this.name}}" class="img-fluid w-100 h-100" style="object-fit: cover;">
                {{else}}
                  <img src="https://placehold.co/400x300?text=No+Image" class="img-fluid w-100 h-100">
                {{/if}}
              </a>
              
              {{#if this.is_new}}
              <div class="position-absolute top-0 start-0 m-2">
                <span class="badge bg-success">New</span>
              </div>
              {{/if}}
            </div>

            <div class="card-body p-3 d-flex flex-column">
              
              <h5 class="card-title mb-2" style="min-height: 48px;">
                <a href="/products/{{this.id}}" class="text-decoration-none text-dark fw-bold text-uppercase" style="font-size: 1rem; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
                  {{this.name}}
                </a>
              </h5>

              <p class="card-text small text-muted mb-2">
                <i class="bi bi-calendar3"></i> Posted: {{format_date this.created_at}}
              </p>

              <hr class="my-2">

              <div class="mb-2">
                <span class="small text-muted">Current Price:</span>
                <div class="h5 text-primary fw-bold mb-0">
                  $ {{format_number this.current_price}}
                </div>
              </div>

              <div class="mb-2">
                <span class="small text-muted">Buy Now:</span>
                
                {{#if this.buy_now_price}}
                  <span class="fw-bold text-success">$ {{format_number this.buy_now_price}}</span>
                {{else}}
                  <span class="text-muted fst-italic">N/A</span>
                {{/if}}
              </div>

              <div class="mb-2 d-flex justify-content-between align-items-center bg-light p-2 rounded">
                <span class="small text-muted">Highest Bidder:</span>
                {{#if this.bidder_name}}
                  <span class="fw-bold text-dark">{{this.bidder_name}}</span>
                {{else}}
                  <span class="small text-muted fst-italic">No bids yet</span>
                {{/if}}
              </div>

              <div class="d-flex justify-content-between mt-auto">
                <div class="text-center border-end pe-3 w-50">
                  <small class="text-muted d-block">Bids</small>
                  <span class="fw-bold">{{this.bid_count}}</span>
                </div>

                <div class="text-center ps-3 w-50">
                  <small class="text-muted d-block">Time Left</small>
                  <span class="fw-bold text-danger countdown-timer" data-end-date="{{this.end_at}}">
                    {{time_remaining this.end_at}}
                  </span>
                </div>
              </div>

              <a href="/products/{{this.id}}" class="btn btn-outline-primary w-100 mt-3">
                View Details
              </a>

            </div>
          </div>
        </div>
        {{/each}}
      </div>
      {{/if}}

      <!-- Pagination -->
      <div class="pagination-wrapper mt-5 mb-4 d-flex justify-content-center align-items-center gap-2">
        {{#if (gt currentPage 1)}}
          <a href="?id={{categoryId}}&page={{subtract currentPage 1}}" class="pagination-btn pagination-prev">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-left-square" viewBox="0 0 16 16">
              <path fill-rule="evenodd" d="M15 2a1 1 0 0 0-1-1H2a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1zM0 2a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2zm11.5 5.5a.5.5 0 0 1 0 1H5.707l2.147 2.146a.5.5 0 0 1-.708.708l-3-3a.5.5 0 0 1 0-.708l3-3a.5.5 0 1 1 .708.708L5.707 7.5z"/>
            </svg>
            Previous 
          </a>
        {{else}}
          <span class="pagination-btn pagination-prev disabled">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-left-square" viewBox="0 0 16 16">
              <path fill-rule="evenodd" d="M15 2a1 1 0 0 0-1-1H2a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1zM0 2a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2zm11.5 5.5a.5.5 0 0 1 0 1H5.707l2.147 2.146a.5.5 0 0 1-.708.708l-3-3a.5.5 0 0 1 0-.708l3-3a.5.5 0 1 1 .708.708L5.707 7.5z"/>
            </svg>
            Previous
          </span>
        {{/if}}
        
        <div class="pagination-numbers d-flex gap-1">
          {{#each (getPaginationRange currentPage totalPages)}}
            {{#if (eq this.type "number")}}
              {{#if (eq this.number ../currentPage)}}
                <a href="?id={{../categoryId}}&page={{this.number}}" class="pagination-page active">{{this.number}}</a>
              {{else}}
                <a href="?id={{../categoryId}}&page={{this.number}}" class="pagination-page">{{this.number}}</a>
              {{/if}}
            {{else}}
              <span class="pagination-ellipsis">...</span>
            {{/if}}
          {{/each}}
        </div>
        
        {{#if (lt currentPage totalPages)}}
          <a href="?id={{categoryId}}&page={{add currentPage 1}}" class="pagination-btn pagination-next">
            Next
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-right-square" viewBox="0 0 16 16">
              <path fill-rule="evenodd" d="M15 2a1 1 0 0 0-1-1H2a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1zM0 2a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2zm4.5 5.5a.5.5 0 0 0 0 1h5.793l-2.147 2.146a.5.5 0 0 0 .708.708l3-3a.5.5 0 0 0 0-.708l-3-3a.5.5 0 1 0-.708.708L10.293 7.5z"/>
            </svg>
          </a>
        {{else}}
          <span class="pagination-btn pagination-next disabled">
            Next 
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-right-square" viewBox="0 0 16 16">
              <path fill-rule="evenodd" d="M15 2a1 1 0 0 0-1-1H2a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1zM0 2a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2zm4.5 5.5a.5.5 0 0 0 0 1h5.793l-2.147 2.146a.5.5 0 0 0 .708.708l3-3a.5.5 0 0 0 0-.708l-3-3a.5.5 0 1 0-.708.708L10.293 7.5z"/>
            </svg>
          </span>
        {{/if}}
      </div>
      <!-- End Pagination -->
    </div>
  </div>
</div>

<style>
  .product-card {
    transition: transform 0.2s, box-shadow 0.2s;
    border: 1px solid #eee;
  }
  .product-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.1);
  }
  .text-primary { color: #0d6efd !important; }
  .bg-light { background-color: #f8f9fa !important; }

  /* Pagination Styles */
  .pagination-wrapper {
    padding: 20px 0;
  }

  .pagination-btn {
    padding: 10px 16px;
    border: 2px solid #72AEC8;
    color: #72AEC8;
    text-decoration: none;
    border-radius: 4px;
    font-weight: 500;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
  }

  .pagination-btn:hover {
    background-color: #72AEC8;
    color: white;
  }

  .pagination-numbers {
    flex-wrap: wrap;
  }

  .pagination-page {
    min-width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid #D8D8D8;
    color: #717171;
    text-decoration: none;
    border-radius: 4px;
    font-weight: 500;
    transition: all 0.3s ease;
  }

  .pagination-page:hover {
    border-color: #72AEC8;
    color: #72AEC8;
  }

  .pagination-page.active {
    background-color: #72AEC8;
    color: white;
    border-color: #72AEC8;
  }

  .pagination-ellipsis {
    display: flex;
    align-items: center;
    color: #717171;
    padding: 0 5px;
    font-weight: 500;
  }

  .pagination-btn.disabled {
    opacity: 0.5;
    cursor: not-allowed;
    border-color: #D8D8D8;
    color: #AEAEAE;
  }

  .pagination-btn.disabled:hover {
    background-color: transparent;
    color: #AEAEAE;
    border-color: #D8D8D8;
  }
</style>